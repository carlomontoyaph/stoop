# Stoop — environment variables
#
# This project requires no environment variables.
# All data is stored in the browser's localStorage — there is no backend.
#
# If you add a backend integration in the future, document new variables here:
# VITE_API_URL=https://your-api.example.com
